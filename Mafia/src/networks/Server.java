package networks;

import game_space.*;
import clients.*;
import java.util.Timer;

public class Server {
	
	public Server(){
		
	}

	public void gameIteration() {
	//check current state via getState
	//if DAY, check if anyone was lynched
		//if so notify players and force update via updateState(switchTime + dayTime)
		//also check win conditions via checkWin()
	//if NIGHT, so same with night
	//call updateState(currentTime)
	}
	
}
